# NOTE: pytest-homeassistant-custom-component is intentionally omitted.
# Our test suite mocks the HA runtime via sys.modules (see conftest.py) and does not
# use any real HA fixtures. Removing it resolves an upstream dependency conflict that
# pins an older pytest version, blocking the CVE-2025-71176 remediation (pytest>=9.0.3).
#
# TODO: Re-evaluate adding it back once it:
#   1. Supports pytest >= 9.0.3 (currently pins to older pytest versions)
#   2. Provides Python 3.14 compatible wheels in CI
#   3. Tracked in: https://github.com/Veldkornet/ha-hyxi-cloud/issues/332
pytest>=9.0.3
pytest-asyncio>=1.3.0
aiohttp>=3.13.4
hypothesis>=6.100.0
hyxi-cloud-api==1.1.5
