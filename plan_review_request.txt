1. Define a utility function `is_null_value(value: Any) -> bool` in `custom_components/hyxi_cloud/const.py`.
```python
def is_null_value(value: Any) -> bool:
    """Check if a value is considered null or equivalent."""
    return value is None or (isinstance(value, str) and value.strip().lower() in NULL_VALUES)
```
2. Import `is_null_value` in `custom_components/hyxi_cloud/sensor.py`.
3. Replace all occurrences of `value is None or (isinstance(value, str) and value.strip().lower() in NULL_VALUES)` with `is_null_value(value)` in `custom_components/hyxi_cloud/sensor.py`.
There's one occurrence of `if v is not None: if isinstance(v, str) and v.strip().lower() in NULL_VALUES:` on line 883-884, we will also replace it with `if not is_null_value(v):`
4. Update `tests/test_parsers.py` and `tests/test_sensor_logic.py` to import and patch the new `is_null_value` function if needed (e.g. `sensor_mod.is_null_value = const_mod.is_null_value` instead of `NULL_VALUES = const_mod.NULL_VALUES`).
5. Run tests, run `ruff format` and `ruff check`, ensure nothing is broken.
6. Commit the changes and submit.
